{
  "$schema": "http://json-schema.org/schema",
  "$id": "NgStarterComponentsSyncDependencies",
  "title": "NgStarter Components Dependency Sync Options",
  "type": "object",
  "properties": {
    "skipInstall": {
      "type": "boolean",
      "description": "Skip running the package manager install task.",
      "default": false
    }
  }
}
