{
  "$schema": "http://json-schema.org/schema",
  "$id": "NgStarterComponentsNgUpdate",
  "title": "NgStarter Components ng-update Options",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Angular application project to configure. Defaults to the workspace default project or the first application project."
    },
    "skipInstall": {
      "type": "boolean",
      "description": "Skip running the package manager install task.",
      "default": false
    },
    "codexSkill": {
      "type": "boolean",
      "description": "Create or update a local Codex skill for NgStarter UI in .codex/skills/ngstarter-ui.",
      "default": true,
      "x-prompt": {
        "message": "Create or update the local Codex skill for NgStarter UI?",
        "type": "confirmation"
      }
    }
  }
}
